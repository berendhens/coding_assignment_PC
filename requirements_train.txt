# Training pipeline dependencies (run.py and everything it imports)
# Matches the working conda environment this pipeline was
# developed and verified in. Install with:
# pip install -r requirements_train.txt

# IMPORTANT: python version 3.12 or higher
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0
numpy>=2.0.0
pandas>=2.2.0
datasets>=2.19.0
scikit-learn>=1.3.0
pyyaml>=6.0